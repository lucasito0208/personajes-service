springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

spring:
  application:
    name: personaje-service
  data:
    mongodb:
      database: personajesdb
      uri: mongodb://mongo:27017/personajes
      host: localhost
      port: 27017

server:
  port: 8080

